ifeq (arch.o compat.o dma.o entry-armv.o entry-common.o irq.o process.o ptrace.o semaphore.o setup.o signal.o sys_arm.o time.o traps.o irq-arch.o io.o,$(strip $(subst $(comma),:,$(EXTRA_LDFLAGS) $(obj-y))))
FILES_FLAGS_UP_TO_DATE += kernel.o
endif
